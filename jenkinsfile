

node {
 
    stage 'Checkout'
 
    git url: 'https://github.com/amithaval7/Datadog.git'
 
            stage name: 'Plan', concurrency: 1
 
            sh "terraform init"
            sh "terraform apply -var 'Instance_ID=${var.Instance_ID}' -auto-approve "
            sh "terraform --version"
 
 
}